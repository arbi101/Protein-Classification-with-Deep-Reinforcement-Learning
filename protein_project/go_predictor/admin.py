from django.contrib import admin

# Django's administration framework is available, but no project model is
# currently registered. To expose ProteinSequence in /admin/, import it and
# call ``admin.site.register(ProteinSequence)`` here.
