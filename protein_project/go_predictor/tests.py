from django.test import TestCase

# This is Django's conventional application test module. No TestCase subclass
# has been added yet, so ``manage.py test go_predictor`` currently runs 0 tests.
# The repository-level tests/ directory is separate: it contains algorithm
# implementations and experiment scripts used by the web application.
