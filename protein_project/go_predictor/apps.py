from django.apps import AppConfig


class GoPredictorConfig(AppConfig):
    """Register the go_predictor application with Django."""

    # Python import path of the application package.
    name = 'go_predictor'
